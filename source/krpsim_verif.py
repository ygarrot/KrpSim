from Process import *
from random import *
import numpy as np

def krpsim_verif():

def main():
    optimize = parse(sys.argv[1])
    while True:
        time, process = 
        if process not in processe.index():
            exit("Process doesn't exist")
        if not check_stock_for_process(current_process, stock):
            exit("not enough ressources in stock to produce this process")
        for process in  processes:
            if ()
            for key, input in current_process.input.items():
                stock[key] -= input



if __name__ == '__main__':
    main()

